apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tunneld.fullname" . }}-test-connection"
  labels:
    {{- include "tunneld.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tunneld.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
