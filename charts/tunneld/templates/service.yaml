apiVersion: v1
kind: Service
metadata:
  name: {{ include "tunneld.fullname" . }}
  labels:
    {{- include "tunneld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tunneld.selectorLabels" . | nindent 4 }}
